
# 📖 Development Log

## 16 Aug 2025
- Setup new **Spring Boot application** (`UserService`).
- Configured MySQL database `splitwise_db` via `application.properties`.
- Created a **TestController** with `/api/test` endpoint to verify setup.
- Created **User entity class** → automatically generated `users` table in MySQL.
- Implemented **UserRepository** for database operations.
- Implemented **DummyDataLoader** using `CommandLineRunner` to preload data.
- Learned and applied **@Builder** feature of Lombok for cleaner object creation.

---